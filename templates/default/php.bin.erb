#!/bin/bash

# Generated by Chef for <%= node['fqdn'] %>
# Local modifications will be overridden

PHP_VERSIONS="<%= node[:phpversion][:versions_dir] %>" PHPVERSION_DISABLE_COMPLETE=1 source <%= node[:phpversion][:install_dir] %>/php-version.sh && php-version <%= @version %> > /dev/null && php $@
